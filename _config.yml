name: a|r|n|e
markdown: rdiscount
rdiscount:
  extensions:
      - autolink      # greedily urlify links
      - footnotes     # footnotes
      - smart         # typographic substitutions with SmartyPants
pygments: true
pagination: 5
